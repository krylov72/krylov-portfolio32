import React from 'react';
import styled from 'styled-components';

export const Header = () => {
    return (
        <div>
            <StyledHeader>
                <Menu />
            </StyledHeader>
        </div>
    );
};

const StyledHeader = styled.header`
    
`
